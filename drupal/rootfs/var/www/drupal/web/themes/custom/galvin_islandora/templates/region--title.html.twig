{# /** * Template to display a region. * This custom template removes the default div around region content for the
purposes of Foundation grid layout. */ #} {% if content %}
<div class="grid-container">{{ content }}</div>
{% endif %}
